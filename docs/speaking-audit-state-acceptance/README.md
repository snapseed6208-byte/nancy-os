# 口语审核状态修复验收（2026-09-10）

状态：代码修复和本地回归完成；真实验收未全部完成，尚未发布本次修复。

## 本次修复

- 只有有效裁决中的 `faithful === true` 才能标记 `verified`。空对象、缺少字段、错误类型、空响应和网络失败保留为 `unchecked`，不冒充通过。
- 当前候选已被明确拒绝时，修复请求失败不撤销拒绝；被拒绝的答案仍被清空。若实际生成了新候选，它需要重新审核。
- 审核状态随现有 JSONB 保存、回读；页面明确显示未核对提示，不再对这类答案声称已保留原意。
- 原意通过但教学说明被指出有误时，保留答案、隐藏教学内容并提示重新分析；没有自动宣称这些说明已修好。
- 页面展示生成、原意核对、修复、独立性检查等进度；没有声称模型等待时间已经缩短。

## 验证

- 全量 Vitest：33 文件、772 项通过。
- `npm run build`：TypeScript 与生产构建通过，无占位符；保留既有大包警告。
- 新增覆盖：无裁决不盖章、裁决类型错误、模型自报状态不可信、明确拒绝后修复失败、存储回读未核对状态、页面提示与教学降级显示。
- 实测 A–F 的统一源码指纹：`a7d40203a72ded9d5547e9f4bbf7ef21b2fa5678094d12fe47c363059e83a132`。指纹覆盖 pipeline、prompts、normalizer、client；每份 JSON 保留执行时间和真实调用结果。没有混用旧目录的案例。

| 案例 | 最佳表达状态 | 参考状态 | 总耗时 |
|---|---|---|---|
| A | unchecked | verified | 148 秒 |
| B | verified | verified | 170 秒 |
| C | unchecked | verified | 108 秒 |
| D | unchecked | verified | 83 秒 |
| E | unchecked | verified | 117 秒 |
| F | verified | verified | 124 秒 |

人工检查：A 保留做饭放松和家人分享；B、F 保持短回答不编造展开；C 保留运动这一主要原因；D 删除无关内容；E 修正破碎表达。参考路线分别为解题尝试、按任务选择办公方式、不受打扰的私人时间、创造与社交、生活节奏、团队纠偏。这里的 `verified` 是模型审核状态，不是质量保证。

## 阻塞与继续位置

G-city 的真实生成请求返回 HTTP 502，上游错误为 `Insufficient Balance`（AI 服务账户余额不足）。因此 G-city、G-tv 未完成，本次完整录音→转写→反馈→数据库 E2E 也未通过。不得引用旧的 audio-e2e.json 作为本次通过证据。

浏览器已打开开发专用虚拟麦克风页面并尝试录音，随后浏览器控制返回 `Debugger unattached`，未确认录音/转写结果；这不构成 E2E 成功。

余额恢复后：运行 `npx tsx scripts/speaking-live-acceptance.ts semantic G-city,G-tv`，确认指纹仍一致；再执行 `audio` 模式及页面录音保存回读。若核心源码改变，需要完整重跑，不能复用本批次冒充同版验收。

推理审核偶发空响应尚未根治。当前系统对这一故障诚实降级；本次不能宣称 8/8 审核通过或全部上线完成。完成剩余验收后才推送发布并检查线上版本。
